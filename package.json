{
  "name": "@carrismetropolitana/deploy-emds",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check": "npm run typecheck && npm run build",
    "dev": "dotenv-run -f ./environments/development/secrets/.env -- turbo run dev --filter=@carrismetropolitana/deploy-emds-api",
    "repo:rinse": "npx --yes @tmlmobilidade/repo-rinse",
    "start": "dotenv-run -f ./environments/production/secrets/.env -- turbo run start --filter=@carrismetropolitana/deploy-emds-api",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@dotenv-run/cli": "^1.3.6",
    "turbo": "^2.9.6"
  },
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "apps/*"
  ]
}
